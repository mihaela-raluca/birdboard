<!DOCTYPE html>
<html lang="en">
    <head>
        <title>{{ "Project " . $project->id }}</title>
    </head>
    <body>
        <h2>{{ $project->title }}</h2>
        <p>{{ $project->description }}</p>
    </body>
</html>
